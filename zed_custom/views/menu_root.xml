<?xml version="1.0" encoding="UTF-8"?>
<odoo>
  <data noupdate="0">

    <!-- App root: The Zed -->
    <menuitem id="menu_zed_root" name="The Zed" sequence="60"/>

    <!-- (A) Re-parent các menu đã có sẵn ở file khác về dưới The Zed -->
    <!-- Nếu trước đó đã tạo menu 'menu_zed_picking_consume' ở stock_picking_views.xml,
         chỉ cần re-parent bằng menuitem, KHÔNG dùng <record model="ir.ui.menu"> -->
    <menuitem id="menu_zed_picking_consume" parent="menu_zed_root" sequence="20"/>

    <!-- Ẩn menu cũ nếu từng đặt ở Inventory (tránh trùng lặp). 
         Lưu ý: thay 'menu_zed_inventory_root' đúng XML ID menu mà bạn muốn ẩn. -->
    <menuitem id="menu_zed_inventory_root" groups="base.group_no_one"/>

    <!-- (B) Khai báo actions + menu tập trung dưới The Zed -->

    <!-- Công thức pha chế -->
    <record id="action_zed_recipe" model="ir.actions.act_window">
      <field name="name">Công thức</field>
      <field name="res_model">zed.recipe</field>
      <field name="view_mode">list,form</field>
    </record>
    <menuitem id="menu_zed_recipe"
              name="Công thức"
              parent="menu_zed_root"
              action="action_zed_recipe"
              sequence="10"/>

    <!-- Đơn hàng POS -->
    <record id="action_zed_pos_orders" model="ir.actions.act_window">
      <field name="name">Đơn hàng POS</field>
      <field name="res_model">pos.order</field>
      <field name="view_mode">list,form</field>
    </record>
    <menuitem id="menu_zed_pos_orders"
              name="Đơn hàng POS"
              parent="menu_zed_root"
              action="action_zed_pos_orders"
              sequence="30"/>

    <!-- Khách hàng -->
    <record id="action_zed_contacts" model="ir.actions.act_window">
      <field name="name">Khách hàng</field>
      <field name="res_model">res.partner</field>
      <field name="view_mode">list,form</field>
      <!-- Có thể thêm domain nếu muốn chỉ khách hàng:
           <field name="domain">[('customer_rank','>',0)]</field> -->
    </record>
    <menuitem id="menu_zed_contacts"
              name="Khách hàng"
              parent="menu_zed_root"
              action="action_zed_contacts"
              sequence="40"/>

    <!-- Nhân viên -->
    <record id="action_zed_employees" model="ir.actions.act_window">
      <field name="name">Nhân viên</field>
      <field name="res_model">hr.employee</field>
      <field name="view_mode">list,form</field>
    </record>
    <menuitem id="menu_zed_employees"
              name="Nhân viên"
              parent="menu_zed_root"
              action="action_zed_employees"
              sequence="50"/>

  </data>
</odoo>
