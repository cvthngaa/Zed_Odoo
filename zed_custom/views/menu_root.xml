<?xml version="1.0" encoding="UTF-8"?>
<odoo>
  <data noupdate="0">

    <!-- App root: The Zed -->
    <menuitem id="menu_zed_root" name="The Zed" sequence="5"/>

    <!-- Phiếu tiêu hao (đã có action_zed_picking_consume ở file khác) -->
    <!-- <menuitem id="menu_zed_picking_consume_root"
              name="Phiếu tiêu hao"
              parent="menu_zed_root"
              action="action_zed_picking_consume"
              sequence="20"/> -->

    <!-- Công thức (BoM) -->
    <record id="action_zed_bom" model="ir.actions.act_window">
      <field name="name">Công thức (BoM)</field>
      <field name="res_model">mrp.bom</field>
      <field name="view_mode">list,form</field>
    </record>
    <menuitem id="menu_zed_bom"
              name="Công thức (BoM)"
              parent="menu_zed_root"
              action="action_zed_bom"
              sequence="10"/>

    <!-- Đơn hàng POS -->
    <record id="action_zed_pos_orders" model="ir.actions.act_window">
      <field name="name">Đơn hàng POS</field>
      <field name="res_model">pos.order</field>
      <field name="view_mode">list,form</field>
    </record>
    <menuitem id="menu_zed_pos_orders"
              name="Đơn hàng POS"
              parent="menu_zed_root"
              action="action_zed_pos_orders"
              sequence="30"/>

    <!-- Khách hàng -->
    <record id="action_zed_contacts" model="ir.actions.act_window">
      <field name="name">Khách hàng</field>
      <field name="res_model">res.partner</field>
      <field name="view_mode">list,form</field>
    </record>
    <menuitem id="menu_zed_contacts"
              name="Khách hàng"
              parent="menu_zed_root"
              action="action_zed_contacts"
              sequence="40"/>

    <!-- Nhân viên -->
    <record id="action_zed_employees" model="ir.actions.act_window">
      <field name="name">Nhân viên</field>
      <field name="res_model">hr.employee</field>
      <field name="view_mode">list,form</field>
    </record>
    <menuitem id="menu_zed_employees"
              name="Nhân viên"
              parent="menu_zed_root"
              action="action_zed_employees"
              sequence="50"/>

    <!-- 🔹 Action Báo cáo tiêu hao (KHAI BÁO TRƯỚC MENU để tránh missing external id) -->
    <record id="action_zed_consumption_report" model="ir.actions.act_window">
      <field name="name">Báo cáo tiêu hao</field>
      <field name="res_model">zed.consumption.report</field>
      <field name="view_mode">list,graph,pivot</field>
      <!-- nếu bạn đã có search view riêng, thêm:
      <field name="search_view_id" ref="view_zed_consumption_report_search"/>
      -->
    </record>

    <!-- Menu Báo cáo tiêu hao -->
    <menuitem id="menu_zed_consumption_report"
              name="Báo cáo tiêu hao"
              parent="menu_zed_root"
              action="action_zed_consumption_report"
              sequence="60"/>

  </data>
</odoo>
